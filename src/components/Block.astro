<div class="block">
    <slot />
</div>

<style>
    .block {
        display: flex;
        padding: 0.5rem 1rem;
        gap: 0.5rem;
        background-color: var(--black);
        color: var(--white);
        font-size: var(--text-md);
        line-height: 1.35;
        white-space: nowrap;
    }
</style>
